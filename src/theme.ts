import { createTheme } from '@mui/material';

export default createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#00A651',
      contrastText: '#FFFFFF',
    },
    secondary: {
      // Discord blue
      main: '#404EED',
      contrastText: '#FFFFFF',
    },
  },
});
